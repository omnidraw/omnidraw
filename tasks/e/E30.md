# E30 - actor resources: evaluate independent files for KV and secret stores
[Overview](../BASED.md)

Evaluate whether each `KvResource` and `SecretStoreResource` should use an independent host-managed database file instead of storing entries in the Vibecanvas control database.

## Context

The current resource model provides logical isolation through `actor_resource_key_values` keyed by `(resource_id, key)`. `KvResource` and `SecretStoreResource` share this table in `DbServiceTurso`. A `DbResource` instead owns a separate physical Turso database at `<dataRoot>/actor-resources/db/<resource-id>/data.db`.

Independent files could improve failure isolation, per-resource backup/restore/export, deletion, future forks, and storage-specific encryption. They also add provisioning/deletion sagas, per-resource schema migration, connection lifecycle and eviction, startup reconciliation, many-small-file overhead, and loss of atomicity with the control catalog.

A separate plaintext secret database does not materially improve confidentiality by itself. Secret-store separation is most valuable when paired with encryption and host key management. KV separation should be driven by concrete requirements such as large datasets, resource export/forking, independent retention, or failure isolation.

Relevant files:
- `packages/service-actor/src/KvResource.ts`
- `packages/service-actor/src/SecretStoreResource.ts`
- `packages/service-actor/src/DbResource.ts`
- `packages/service-actor/src/ActorResourceManager.ts`
- `packages/service-db/src/DbServiceTurso/DbServiceTurso.ts`

## Plan

1. Measure expected resource count, entry volume, and open-handle/file overhead.
2. Compare shared-table and per-resource-file failure, backup, restore, export, and deletion behavior.
3. Prototype narrow KV and secret persistence ports so storage can change without affecting actor manifests, bindings, IPC, or SDK APIs.
4. Define lazy connection opening, handle eviction, physical schema versioning, and crash reconciliation for per-resource files.
5. Evaluate secret storage together with encryption-at-rest and OS keychain/master-key handling.
6. Decide independently for KV and secret stores; they do not need to use the same physical strategy.

## TODOS

- [ ] Document concrete product requirements for independent resource backup/export/forking.
- [ ] Benchmark many small Turso files and lazy handle management.
- [ ] Design per-resource provisioning, deletion, migration, and recovery sagas.
- [ ] Design narrow persistence ports for KV and secret providers.
- [ ] Evaluate encrypted secret storage and host key management.
- [ ] Record keep-shared versus move-to-files decisions separately for KV and secret stores.

## NOTES

Current recommendation: keep KV in the shared control database until physical independence has a concrete use case. Consider moving secret stores only as part of a deliberate encrypted-storage design. Preserve provider-facing storage abstractions so either backend can change later.

### LOGS

- Added exploration after reviewing the current shared resource-key-value table and per-DbResource physical file model.

---
