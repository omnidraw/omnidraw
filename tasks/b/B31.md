# B31 - binary: compiled app cannot find Turso native binding
[Overview](../BASED.md)

Compiled `vibecanvas` starts with `Cannot find native binding` because the Turso NAPI `.node` file is not available at runtime.

## Context

`bun run build:single` succeeds, but running `./dist/vibecanvas-darwin-arm64/bin/vibecanvas` fails in the compiled executable with:

```txt
error: Cannot find native binding. npm has a bug related to optional dependencies...
```

Relevant files:
- `scripts/build.ts` compiles the single-file binary and creates `dist/` packages.
- `apps/cli/src/setup-services.ts` creates `DbServiceTurso` for stateful commands/server.
- `packages/service-db/src/DbServiceTurso/DbServiceTurso.ts` imports `@tursodatabase/database`.
- `docs/external/llm.bun.md` references Bun docs for single-file executables, loaders, plugins, Node-API, and SQLite.

Observed investigation:
- `@tursodatabase/database` uses a NAPI/native platform binding.
- On darwin arm64 the required file is `turso.darwin-arm64.node` from `@tursodatabase/database-darwin-arm64`.
- `bun build --compile` bundles JS but does not make this native binding available to the compiled executable.
- Copying `turso.darwin-arm64.node` into the run working directory allows `--version` to start, confirming the missing native binding is the immediate cause.

## Plan

### 1. Read Bun executable/native docs
- Use `docs/external/llm.bun.md` to find the relevant Bun docs.
- Check single-file executable behavior for file/native assets.
- Check loader/plugin options if native binding relocation needs a build-time shim.

### 2. Decide binary packaging strategy
- Prefer a robust executable-relative or extracted-assets path for native bindings.
- Avoid relying on the caller's current working directory.
- Cover all release targets, not only local darwin arm64.

### 3. Implement the fix
- Update build/runtime packaging so the Turso native binding is available for compiled binaries.
- Ensure local dev and non-compiled Bun execution keep working.
- Keep generated/copy logic explicit and target-aware.

### 4. Validate the actual binary
- Run `bun run build:single`.
- Run the built app binary directly from the repository root: `./dist/<current-target>/bin/vibecanvas --version`.
- Run a stateful command or short server smoke test that initializes `DbServiceTurso`.
- Confirm the app binary no longer requires manual `.node` copying.

## TODOS

- [ ] Read relevant Bun docs through `docs/external/llm.bun.md` refs.
- [ ] Identify every platform-specific Turso native binding package needed by release targets.
- [ ] Choose executable-relative/extracted native binding loading strategy.
- [ ] Update build/runtime packaging for compiled binaries.
- [ ] Verify normal source-mode CLI still works.
- [ ] Run `bun run build:single`.
- [ ] Run the built app binary from repo root.
- [ ] Run a stateful binary smoke test that opens/creates the database.
- [ ] Document any release packaging implications.

## NOTES

- Non-fatal warning seen during startup: `using deprecated parameters for initSync()` appears separate from this failure.
- Existing portability warning for `/home/runner/work/` is also separate unless native binding packaging changes reveal a related issue.

### LOGS

- Created from investigation after `bun run build:single` produced a binary that failed at startup with missing `@tursodatabase/database` native binding.

---
