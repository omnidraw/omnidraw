# S70 - ai wizard: remove default slash and mention values
[Overview](../../BASED.md)

Remove hardcoded slash-command and mention default values from the AI wizard chat tab while keeping the menu code available.

## Context
In `packages/canvas/src/components/AiWizzard/tabs/ChatTab.tsx`, slash and mention menu behavior should remain implemented, but default values should not be preconfigured/hardcoded.

Relevant file:
- `packages/canvas/src/components/AiWizzard/tabs/ChatTab.tsx`

## Plan
1. Inspect slash-command and mention menu setup in `ChatTab.tsx`.
2. Find hardcoded default slash and mention values.
3. Remove/disable the default values without deleting menu support code.
4. Verify chat input still renders and menu code can be populated later.

## TODOS
- [x] Locate slash-command default values in `ChatTab.tsx`.
- [x] Locate mention default values in `ChatTab.tsx`.
- [x] Remove hardcoded defaults while preserving menu plumbing.
- [x] Smoke-check AI wizard chat input behavior.

## NOTES
- The hardcoded slash-command and mention values live in `packages/canvas/src/components/AiWizzard/ChatComposer/ChatComposer.tsx`, not `ChatTab.tsx`.
- Removed default slash-command and mention arrays.
- Composer menu plumbing remains intact through optional `commands` and `mentions` props; unconfigured composer instances now use empty suggestion sources.

### LOGS
- Created task from user request.
- Implemented S70 in `ChatComposer.tsx`.
- Ran `git diff --check`: passed.
- Ran focused AI wizard tests: `bun x vitest --run tests/components/AiWizzard/ChatComposer/fn.trigger.test.ts tests/components/AiWizzard/tabs/ChatTab.render.test.ts`: passed.

---
