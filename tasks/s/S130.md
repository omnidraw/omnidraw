# S130 - remove marketing/docs website and GitHub Pages publishing
[Overview](../BASED.md)

Remove the retired Astro website and every live repository integration that builds, tests, documents, or publishes it.

## Context

The monorepo still contains `apps/web`, a GitHub Pages deployment workflow, root scripts, lockfile packages, publishing documentation, and website URLs that assume the site remains deployed.

Completed task files remain historical records and are not rewritten.

## Plan

1. Delete the website and its GitHub Pages workflow.
2. Remove workspace scripts, docs, and source-scanning support that exist only for the website.
3. Replace live `omnidraw.dev` metadata and installer links with repository-backed URLs.
4. Regenerate the lockfile and `FILES.md`, then run focused and repository-wide checks.

## TODOS

- [x] inventory website and publishing references
- [x] delete `apps/web` and `.github/workflows/deploy-web.yml`
- [x] remove live scripts, docs, metadata, and Astro-only support
- [x] prune the lockfile and regenerate `FILES.md`
- [x] verify no live website references remain and tests pass

## NOTES

- `scripts/local-registry.mjs` has an unrelated Verdaccio `web.enable` option and is intentionally retained.
- Historical task files are intentionally retained as the project audit log.

### LOGS

- 2026-07-31: inventoried exact app references, GitHub Pages configuration, Astro dependencies, website assets, and `omnidraw.dev` links.
- 2026-07-31: deleted the website, Pages workflow, and Astro reference; removed root scripts/test filters and deployment documentation.
- 2026-07-31: replaced live website metadata and installer URLs with GitHub-backed URLs; pruned 575 lockfile lines of site-only dependencies.
- 2026-07-31: `bash -n scripts/install.sh`, `bun run test:architecture`, and the full `bun run test` suite passed.

---
