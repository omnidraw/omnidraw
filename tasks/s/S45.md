# S45 - canvas tests: text edit/session lane
[Overview](../../BASED.md)

Repair the S42 fallout in canvas text plugin tests.

## Context
S42 moved edit state away from old editor-owned fields. Text tests under `packages/canvas/tests/plugins/text/**` still assert old state such as `editor.editingTextId` and some direct helper tests do not inject the current portal dependencies.

Current baseline from 2026-04-23:
- `bunx vitest run tests` in `packages/canvas`
- 40 files total
- 148 tests total
- 8 failing files
- 11 failing tests

Known failures in this lane:
- `tests/plugins/text/Text.click-create.plugin.test.ts`
- `tests/plugins/text/Text.core.plugin.test.ts`
- `tests/plugins/text/Text.viewport-editing.plugin.test.ts`
- `tests/plugins/text/fx.to-element.scale-persistence.test.ts`

## Plan
1. Replace old editor edit-state assertions with current session/edit-mode state.
2. Update direct text serialization tests to provide required portals such as `Date`.
3. Re-check viewport overlay tests against current runtime semantics.
4. Remove stale assertions that only validate removed editor fields.
5. Run the lane command and log the result.

## TODOS
- [x] Update text edit-state assertions.
- [x] Update text helper portal setup.
- [x] Decide whether viewport overlay tests still describe current behavior.
- [x] Remove obsolete tests that target deleted runtime paths.
- [x] Run `bunx vitest run tests/plugins/text` from `packages/canvas`.
- [x] Update this file with the focused result.

## NOTES
- Keep the tests focused on current text behavior, not old editor implementation details.

### LOGS
- 2026-04-23: Lane created from S42 test repair split.
- 2026-04-23: Replaced stale text tests that required removed `editor`/`canvasRegistry` services with current `tool`, `element`, and observable textarea overlay behavior.
- 2026-04-23: Added current `Date` portal to direct `fxToElement` test setup.
- 2026-04-23: `bunx vitest run tests/plugins/text` from `packages/canvas` passed: 6 files, 25 tests.

---
