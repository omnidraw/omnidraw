# S62 - reimplement terminal widget API path
[Overview](../BASED.md)

Rebuild the terminal widget integration after S60 disabled the broken surface.

## Context

The terminal widget implementation still exists, but it was removed from the active runtime path because the current API wiring is broken and causes runtime/test harness failures. Keep the feature on the roadmap, but reintroduce it only after the API contract and client surface are made coherent.

Relevant areas to inspect:
- `packages/canvas/src/plugins/terminal`
- `packages/canvas/src/runtime.ts`
- `packages/api-pty`
- `packages/orpc-client`
- `apps/cli/src/plugins/orpc`

## Plan

### Audit API contract

Trace the current terminal widget calls against the pty/orpc contracts and identify the broken or stale assumptions.

### Rebuild integration boundary

Update the widget to use the current API client shape, with explicit handling for filesystem/session identity and connection lifecycle.

### Re-enable surface

Restore terminal widget registration in the runtime only after the widget works against the current API path.

## TODOS

- [ ] Audit terminal widget API calls and pty/orpc contract shape.
- [ ] Fix the terminal widget client integration and lifecycle behavior.
- [ ] Add or update focused tests for terminal widget API behavior.
- [ ] Re-enable terminal widget runtime registration.
- [ ] Verify terminal widget creation from the UI.

## NOTES

- S60 intentionally commented out the active surface without deleting implementation.
- Do not simply uncomment S60 unless the API path is verified.

### LOGS

---
