# S68 - ai wizard chat running indicator
[Overview](../../BASED.md)

Show a clear working indicator while an AI wizard chat run is active.

## Context
`packages/canvas/src/components/AiWizzard/tabs/ChatTab.tsx` needs UI feedback when chat is running. Users should see that the assistant is working after sending a message or while a run is in progress.

Relevant file:
- `packages/canvas/src/components/AiWizzard/tabs/ChatTab.tsx`

## Plan
1. Inspect ChatTab run/loading state and existing actor/chat status fields.
2. Pick the most reliable active-run signal.
3. Add a small visible working indicator during active chat runs.
4. Ensure the indicator clears on completion, error, and cancel.

## TODOS
- [x] Inspect `ChatTab.tsx` run state and message send flow.
- [x] Identify existing loading/running status source.
- [x] Add working indicator UI while chat is running.
- [x] Verify send, completion, error, and cancel states.

## NOTES
- `ChatTab.tsx` already receives `isRunning` and `isCanceling` from `AiWizzard`.
- `AiWizzard` sets `isRunning` on prompt start and agent stream events, clears it on prompt completion/error, and updates it from cancel response state.
- The chat now renders an inline `role="status"` working indicator while `isRunning` is true, with cancel-specific copy while `isCanceling` is true.
- Verification: `bun --filter @vibecanvas/canvas test` currently fails in unrelated widget host/portal tests.
- Verification: `bun x tsc -p packages/canvas/tsconfig.json --noEmit` currently fails on existing canvas/project type errors unrelated to the changed chat files.

### LOGS
- Created task from user request.
- Implemented chat running/canceling indicator in `ChatTab.tsx` and `index.css`.
- Ran `git diff --check`: passed.
- Ran `bun --filter @vibecanvas/canvas test`: failed in `tests/services/widget/fx.draw-host.test.ts` and `tests/services/widget/tx.attach-dom-portal.fullscreen.test.ts`.
- Ran `bun x tsc -p packages/canvas/tsconfig.json --noEmit`: failed on existing project type errors.

---
