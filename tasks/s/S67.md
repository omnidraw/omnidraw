# S67 - floating menu only when element is selected
[Overview](../../BASED.md)

Keep drawing area free by hiding the floating menu during tool-only selection.

## Context
The floating menu currently appears when a drawing tool is selected, even if no canvas element is selected. This blocks drawing space and should only appear for an actual selected element.

Relevant area to inspect when implementing:
- canvas selection state
- floating/style menu visibility logic
- drawing tool selection UI

Source files located:
- `packages/canvas/src/plugins/selection-style-menu/fx.mount-selection-style-menu.ts`
- `packages/canvas/tests/plugins/selection-style-menu/SelectionStyleMenu.plugin.test.ts`

## Plan
1. Find the floating/style menu visibility condition.
2. Separate tool selection state from element selection state.
3. Show the floating menu only when at least one element is selected.
4. Verify drawing tools no longer open the menu by themselves.

## TODOS
- [x] Locate floating menu component/service visibility logic.
- [x] Update task with exact source files before coding.
- [x] Implement visibility based on selected element(s), not active tool.
- [x] Verify with rect/pen/text tools and selected elements.

## NOTES
Requested with screenshot reference from clipboard temp path; no product code edited in BASED mode.

### LOGS
- Created task from user request.
- Located selection style menu visibility fallback to active tool config in `fx.mount-selection-style-menu.ts`.
- Implemented selected-element-only visibility and updated selection-style-menu regression tests.
- Verified with `bun --filter @vibecanvas/canvas test tests/plugins/selection-style-menu/SelectionStyleMenu.plugin.test.ts` and `bun run lint:functional-core`.

---
