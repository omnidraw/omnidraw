# S105 - remove scoped_events table and related code
[Overview](../../BASED.md)

Remove the `scoped_events` table and all related code. Edit the existing SQL file directly (no new migration — nothing deployed yet).

## Context

The `scoped_events` table is unused or deprecated. Remove it and any code that references it.

## Plan

### Step 1: Remove SQL table

Edit the existing SQL file directly to remove the `scoped_events` table definition, indexes, and constraints.

### Step 2: Remove related code

Search for and remove all references to `scoped_events` across the codebase: models, repositories, services, API contracts, tests, and any other usage.

## TODOS

- [x] Edit existing SQL file to remove scoped_events table
- [x] Remove scoped_events model/repository code
- [x] Remove scoped_events service/API references
- [x] Remove scoped_events tests

## NOTES

- Edit the existing SQL file directly — no new migration needed since nothing has been deployed.

### LOGS

- Removed the table, indexes, and expected-schema entries from the unreleased initial migration.
- Removed the public scoped-event runtime contract and managed-composition fixture coverage.
- Kept the active service-owned DB, actor, agent, and notification event channels; renamed their private in-memory helper from `ScopedEventBus` to `EventBus`.
- Updated the stale evidence generator call signature, regenerated its database introspection output, and verified the combined S103-S105 initial migration's 37-table baseline.

---
