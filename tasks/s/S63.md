# S63 - reimplement filesystem widget API path
[Overview](../BASED.md)

Rebuild the filesystem widget integration after S61 disabled the broken surface.

## Context

The filesystem widget implementation still exists, but it was removed from the active runtime path because the current API wiring is broken and needs a clean pass against the filesystem/orpc contracts. Keep the feature visible for later reintroduction.

Relevant areas to inspect:
- `packages/canvas/src/plugins/filesystem`
- `packages/canvas/src/runtime.ts`
- `packages/api-filesystem`
- `packages/orpc-client`
- `apps/cli/src/plugins/orpc`
- `packages/service-filesystem`

## Plan

### Audit API contract

Trace the current filesystem widget calls against the filesystem/orpc contracts and identify stale assumptions around root selection, reads, writes, watches, and filesystem identity.

### Rebuild integration boundary

Update the widget to use the current API client shape with explicit error handling and watch lifecycle cleanup.

### Re-enable surface

Restore filesystem widget registration in the runtime only after the widget works against the current API path.

## TODOS

- [ ] Audit filesystem widget API calls and filesystem/orpc contract shape.
- [ ] Fix the filesystem widget client integration and watch lifecycle behavior.
- [ ] Add or update focused tests for filesystem widget API behavior.
- [ ] Re-enable filesystem widget runtime registration.
- [ ] Verify filesystem widget creation from the UI.

## NOTES

- S61 intentionally commented out the active surface without deleting implementation.
- Do not simply uncomment S61 unless the API path is verified.

### LOGS

---
