# S55 - store images as blob not base64
[Overview](../../BASED.md)

Store image media in Turso as binary blobs instead of base64 text, and rename the `files` table to `media_files` before this migration ships.

## Context
Current image storage work should target the existing, undeployed migration file only:
- `packages/service-db/src/DbServiceTurso/migration-files/003-add-media-files.sql`

Do not create a new migration because this schema has not deployed yet. Rewrite migration `003-add-media-files.sql` in place.

Required schema direction:
- Rename table `files` -> `media_files`.
- Replace text/base64 payload storage with a Turso-supported `BLOB` column.
- Use Turso blob behavior from `docs/external/llm.turso.md`.

Primary end-to-end use case:
- Image elements only.
- Canvas image plugin path: `packages/canvas/src/plugins/image`.

This must work through the full stack, not just the schema layer.

## Plan

### 1. Map current image flow end to end
- Find every read/write path that references the current `files` table or image base64 storage.
- Trace image creation from `packages/canvas/src/plugins/image` through canvas services, API/orpc, db service, and back to render/hydration.
- Identify the data contract currently passed around: file id, mime/type, filename, width/height metadata, base64 text, data URL, or URL endpoint.
- Record which parts are persisted in the canvas CRDT element data versus stored in the database.

### 2. Rewrite migration 003 in place
- Edit `packages/service-db/src/DbServiceTurso/migration-files/003-add-media-files.sql` only; do not add migration `004`.
- Rename the table from `files` to `media_files`.
- Replace any base64/text content column with a blob column.
- Keep required metadata columns needed by image elements, such as id, mime type, size, created timestamp, and any existing ownership/canvas references.
- Update indexes/constraints/triggers in the same migration to use `media_files` names.

### 3. Update db service contracts
- Rename db methods/types from generic file naming to media-file naming where it improves clarity.
- Ensure writes accept binary data (`ArrayBuffer`, `Uint8Array`, or `Blob` depending on current stack conventions) instead of base64 text.
- Ensure reads return binary data or a response/stream suitable for browser image rendering.
- Ensure no server path converts binary image bytes into base64 for storage.

### 4. Update API/orpc routes
- Update routes that upload, fetch, or reference stored image content to use `media_files`.
- Make request/response payloads binary-safe.
- Preserve image element references by id/URL instead of embedding base64 in element data.
- Confirm client fetch/render path can load the blob-backed image.

### 5. Update canvas image plugin
- In `packages/canvas/src/plugins/image`, remove assumptions that persisted images are base64/data URL text.
- Keep image element data small: store media file id and render URL/reference, not raw image bytes.
- On create/paste/upload, send binary image bytes to the stack and persist the returned media id/reference in the element.
- On render/hydrate/reload, resolve media id/reference and display the image.

### 6. Migration/reset expectations
- Because migration `003` is undeployed, reset local/dev databases as needed instead of writing compatibility migrations.
- Check seed/test fixtures that expect table `files` or base64 text and update them to `media_files` + blob.

### 7. End-to-end verification
- Run the stack with all related parts needed for image elements.
- Create or paste an image element.
- Confirm DB row is created in `media_files` with blob bytes, not base64 text.
- Reload the canvas and confirm the image still renders.
- Move/resize/delete normal image element flows and confirm no regressions.
- If there is a public image fetch endpoint, open it directly and confirm it serves correct content type and bytes.

### 8. Tests/checks
- Add/update db tests for inserting and reading blob-backed media files.
- Add/update API tests for image upload/fetch if existing harness covers it.
- Add/update image plugin tests only where current test setup can cover the behavior without excessive integration complexity.
- Run package checks and the relevant app stack before marking done.

## TODOS
- [x] Audit all references to `files` table and base64 image storage.
- [x] Read `docs/external/llm.turso.md` and apply Turso blob guidance.
- [x] Rewrite `003-add-media-files.sql` in place: `files` -> `media_files`, text/base64 -> blob.
- [x] Update service-db types, queries, and tests for blob media files.
- [x] Update API/orpc upload/fetch contracts to be binary-safe.
- [x] Update `packages/canvas/src/plugins/image` to store media references, not base64 content.
- [x] Reset local dev DB if needed because migration 003 is intentionally rewritten.
- [x] Run the full related stack and verify image create/persist/reload end to end.
- [x] Document exact commands run and verification results in this leaf.

## NOTES
- No new migration for this work unless deployment history changes; migration `003-add-media-files.sql` is not deployed yet.
- Table name target is `media_files`.
- Storage target is Turso `BLOB`, not text base64.
- Current required product scope is image elements only.
- Implemented path keeps image element CRDT data small: new images persist a `/files/:id.ext` URL and `base64: null`; binary bytes travel through the file API as `Uint8Array`.
- Local dev DB reset was not needed in this worktree; verification used in-memory migration tests and the full build regenerated embedded migrations from the rewritten `003`.
- Manual browser create/reload was not run; automated coverage now verifies blob schema/storage, HTTP byte serving, and image paste/insert upload payloads.

### LOGS
- Created leaf from S55 note request with detailed implementation and verification plan.
- 2026-06-30: Audited current image flow: canvas image plugin uploaded base64 through `api.file.put`, `api-file` decoded bytes for hashing but stored base64 text in DB, CLI HTTP re-decoded base64 for `/files/:id.ext`, and CRDT image elements stored URL plus nullable base64 fallback.
- 2026-06-30: Rewrote `packages/service-db/src/DbServiceTurso/migration-files/003-add-media-files.sql` in place: table is now `media_files`, payload column is `data BLOB`, and hash index is `media_files_hash_idx`.
- 2026-06-30: Renamed migration file from `003-add-files.sql` to `003-add-media-files.sql` and updated the migration registry.
- 2026-06-30: Updated service-db model/query surface to use `TMediaFile` with `data: Uint8Array | ArrayBuffer`, and updated file tx/fx queries to read/write `media_files`.
- 2026-06-30: Updated file API upload contract from `base64` string to `data: Uint8Array`; upload now hashes/stores bytes directly and clone copies blob data.
- 2026-06-30: Updated CLI `/files/:id.ext` serving to fetch media bytes asynchronously and return them directly with the stored MIME type.
- 2026-06-30: Updated canvas image insert path to read bytes via injected `FileReader`, upload `Uint8Array`, and keep element data as URL reference with `base64: null`.
- 2026-06-30: Verification passed: `bun test apps/cli/tests/server.http.test.ts`; `bun --filter @vibecanvas/service-db test`; `bun --filter @vibecanvas/canvas test tests/plugins/image/Image.plugin.test.ts tests/plugins/image/tx.insert-image.test.ts`; `bun run lint:functional-core:agent`; `bun run build`; `bun run test`.
- 2026-06-30: Fixed paste/upload RPC validation failure (`expected Uint8Array, received object`) by sending image bytes as ORPC-supported `Blob` over websocket, accepting `Blob | Uint8Array` in the file contract, and converting uploads back to `Uint8Array` before hashing/storing.

---
