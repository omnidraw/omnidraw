# S60 - remove terminal widget (comment out only)
[Overview](../BASED.md)

Disable the terminal widget surface without deleting the implementation yet.

## Context

The terminal widget should be removed from the active UI/runtime path for now, but the code should remain available behind comments so it can be restored or referenced during follow-up cleanup.

Relevant areas to inspect:
- widget registration/discovery
- terminal plugin/widget wiring
- any menu or command that creates terminal widgets

## Plan

### Disable entry points

Comment out registration, creation actions, and menu/discovery entries that expose terminal widgets.

### Preserve implementation

Do not delete files or migrate data in this task. Leave clear comments explaining the temporary disablement.

### Validate absence

Confirm the terminal widget is no longer offered or instantiated from normal UI paths.

## TODOS

- [x] Find terminal widget registration and creation entry points.
- [x] Comment out active terminal widget registration/exposure only.
- [x] Add short comments at disabled entry points referencing S60.
- [x] Verify existing terminal widget code is not deleted.
- [x] Smoke test that terminal widget is no longer creatable from the UI.

## NOTES

- Scope is intentionally comment-out only, not deletion.

### LOGS

- 2026-06-30: Found the active terminal widget entry point in `packages/canvas/src/runtime.ts`, where `createTerminalPlugin()` was included in the runtime plugin list. The plugin implementation remains in `packages/canvas/src/plugins/terminal`.
- 2026-06-30: Commented out the terminal runtime plugin entry with an S60 note. Also commented out the legacy static toolbar shortcut and toolbar item for `terminal` in `packages/canvas/src/components/FloatingCanvasToolbar/toolbar.types.ts`.
- 2026-06-30: Verified the implementation files still exist and source search finds active terminal creation only in the preserved plugin implementation or commented disabled entries.
- 2026-06-30: Ran `bun --filter @vibecanvas/frontend build` successfully. `bun --filter @vibecanvas/canvas test` still fails on existing harness/test issues unrelated to S60, including missing actor API mocks, stale widget draw-host fixtures, and a Konva image draw error.

---
