# S95 - sidebar: unify selected widget highlight
[Overview](../BASED.md)

Give the active widget row the same clear selected treatment used for canvases and resources.

## Context

Widget rows are rendered by [`packages/ui-ai-chat/src/sidebar/widgets/components/WidgetsSidebarSection.tsx`](../../packages/ui-ai-chat/src/sidebar/widgets/components/WidgetsSidebarSection.tsx) and styled in [`packages/ui-ai-chat/src/sidebar/widgets/components/WidgetsSidebarSection.module.css`](../../packages/ui-ai-chat/src/sidebar/widgets/components/WidgetsSidebarSection.module.css). Selection matching and styles already exist, but the active widget is not visibly highlighted in the supplied widget-detail view.

Canvas and resource selection provide the expected reference behavior in [`packages/ui-ai-chat/src/sidebar/components/Sidebar.tsx`](../../packages/ui-ai-chat/src/sidebar/components/Sidebar.tsx) and [`packages/ui-ai-chat/src/sidebar/components/SidebarItem.module.css`](../../packages/ui-ai-chat/src/sidebar/components/SidebarItem.module.css).

The highlight must identify the exact selected variant when a widget has both published and draft rows with the same display name.

## Plan

1. Diagnose why widget route selection is not producing a visible active row.
2. Make widget selection react correctly to detail-route navigation and distinguish draft from published sources.
3. Align the selected visual state with canvas and resource rows across themes, hover, and keyboard focus.
4. Preserve `aria-current` on only the active row.
5. Add focused selection and rendering tests.

## TODOS

- [x] Fix active widget route matching and reactivity.
- [x] Apply a consistent selected-row highlight.
- [x] Distinguish draft and published rows with the same widget name.
- [x] Verify light/dark, hover, focus, and accessibility states.
- [x] Add focused tests.

## NOTES

- The route matcher and selected class were already present; the missing lifecycle step was revealing a selected row nested inside a collapsed widget group.

### LOGS

- 2026-07-19: Task created from the supplied widget-detail sidebar reference.
- 2026-07-19: Auto-expanded the active widget group from reactive route selection, preserved source-specific matching, aligned selected-row transitions and colors, and added pure matching plus Solid rendering coverage.
- 2026-07-28: Audited completion after the package rename, corrected source links,
  and passed the focused widget selection tests (8/8) plus the
  `@vibecanvas/ui-ai-chat` typecheck.

---
