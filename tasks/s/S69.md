# S69 - ai wizard: save tool before phase2 updates candidate
[Overview](../../BASED.md)

Before phase 2, saving tool metadata should update the actor candidate instead of writing a JSON file directly.

## Context
In `packages/canvas/src/components/AiWizzard/tabs/ChatTab.tsx`, the save-tool flow before phase 2 should persist changes by updating the current candidate state. It should not write/update a JSON file as the source of truth at this point.

Relevant file:
- `packages/canvas/src/components/AiWizzard/tabs/ChatTab.tsx`

## Plan
1. Inspect the pre-phase-2 save-tool flow in `ChatTab.tsx`.
2. Identify where the tool JSON file is currently written or updated.
3. Change the flow to update the current candidate instead.
4. Verify phase 2 reads the updated candidate data.

## TODOS
- [x] Locate save-tool handler; it lives in `ToolTab.tsx`.
- [x] Identify candidate state/update API used by the wizard.
- [x] Replace pre-phase-2 JSON-file update with candidate update.
- [x] Verify phase 2 receives the saved tool metadata from candidate state.

## NOTES
- The actual save-tool handler is in `packages/canvas/src/components/AiWizzard/tabs/ToolTab.tsx`.
- `draftManifest.patch` now patches the latest actor candidate when no draft `vibecanvas.json` exists, appending a new validated candidate revision instead of writing a file.
- Once `vibecanvas.json` exists, `draftManifest.patch` keeps the phase-2 behavior and writes the patched manifest file.
- Patch results now include `source: "actor-candidate" | "file"` so the UI preserves whether the edited manifest is still a candidate.

### LOGS
- Created task from user request.
- Implemented candidate-phase patching in `packages/service-agent/src/AgentService.ts`.
- Updated `packages/api-agent/src/contract.ts` patch result type with manifest source.
- Updated `ToolTab` and AI wizard wiring to preserve the returned manifest source.
- Added/updated service-agent regression coverage for candidate-phase tool metadata saves.
- Ran `git diff --check`: passed.
- Ran `bun --filter @vibecanvas/service-agent test`: passed.
- Ran `bun --filter @vibecanvas/service-agent typecheck`: passed.

---
