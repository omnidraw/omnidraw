# S47 - canvas tests: stale coverage audit and integration lane
[Overview](../../BASED.md)

Audit currently passing canvas tests for stale S42 assumptions and run final integration.

## Context
Some canvas tests still pass because their mocks are broad. This lane owns the lower-failure folders and final integration after S44, S45, and S46 land.

Owned folders:
- `packages/canvas/tests/plugins/camera/**`
- `packages/canvas/tests/plugins/group/**`
- `packages/canvas/tests/plugins/scene-hydrator/**`
- `packages/canvas/tests/plugins/selection-style-menu/**`
- canvas test docs

Current baseline from 2026-04-23:
- `bunx vitest run tests` in `packages/canvas`
- 40 files total
- 148 tests total
- 8 failing files
- 11 failing tests

## Plan
1. Audit owned folders for broad mocks that hide removed service paths.
2. Remove obsolete tests that only cover deleted runtime paths.
3. Add small tests for meaningful S42 gaps that are missing after the other lanes.
4. Run the lane folders.
5. After S44, S45, and S46 are merged, run the full canvas test suite.
6. Update `tasks/s/S42.md` and `packages/canvas/TESTS.md` with final results.

## TODOS
- [ ] Audit camera tests.
- [ ] Audit group tests.
- [ ] Audit scene-hydrator tests.
- [ ] Audit selection-style-menu tests.
- [ ] Run `bunx vitest run tests/plugins/camera tests/plugins/group tests/plugins/scene-hydrator tests/plugins/selection-style-menu` from `packages/canvas`.
- [ ] Run final `bunx vitest run tests` from `packages/canvas` after lane merges.
- [ ] Update S42 and canvas test docs with final status.

## NOTES
- This lane should avoid taking over files already owned by S44, S45, or S46.
- Use this lane to catch stale passing coverage, not to duplicate the failing-test fixes.

### LOGS
- 2026-04-23: Lane created from S42 test repair split.

---
