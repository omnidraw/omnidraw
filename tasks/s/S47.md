# S47 - canvas tests: stale coverage audit and integration lane
[Overview](../../BASED.md)

Audit currently passing canvas tests for stale S42 assumptions and run final integration.

## Context
Some canvas tests still pass because their mocks are broad. This lane owns the lower-failure folders and final integration after S44, S45, and S46 land.

Owned folders:
- `packages/canvas/tests/plugins/camera/**`
- `packages/canvas/tests/plugins/group/**`
- `packages/canvas/tests/plugins/scene-hydrator/**`
- `packages/canvas/tests/plugins/selection-style-menu/**`
- canvas test docs

Current baseline from 2026-04-23:
- `bunx vitest run tests` in `packages/canvas`
- 40 files total
- 148 tests total
- 8 failing files
- 11 failing tests

## Plan
1. Audit owned folders for broad mocks that hide removed service paths.
2. Remove obsolete tests that only cover deleted runtime paths.
3. Add small tests for meaningful S42 gaps that are missing after the other lanes.
4. Run the lane folders.
5. After S44, S45, and S46 are merged, run the full canvas test suite.
6. Update `tasks/s/S42.md` and `packages/canvas/TESTS.md` with final results.

## TODOS
- [x] Audit camera tests.
- [x] Audit group tests.
- [x] Audit scene-hydrator tests.
- [x] Audit selection-style-menu tests.
- [x] Run `bunx vitest run tests/plugins/camera tests/plugins/group tests/plugins/scene-hydrator tests/plugins/selection-style-menu` from `packages/canvas`.
- [x] Run final `bunx vitest run tests` from `packages/canvas` after lane merges.
- [x] Update S42 and canvas test docs with final status.

## NOTES
- This lane should avoid taking over files already owned by S44, S45, or S46.
- Use this lane to catch stale passing coverage, not to duplicate the failing-test fixes.

### LOGS
- 2026-04-23: Lane created from S42 test repair split.
- 2026-04-23: Audited owned folders. Replaced stale `editor` service lookups with current `tool` service in camera and selection-style-menu tests, and replaced stale group regression `canvasRegistry.createNodeFromElement` usage with `element.createNodeFromElement`.
- 2026-04-23: Focused S47 run passed: `bunx vitest run tests/plugins/camera tests/plugins/group tests/plugins/scene-hydrator tests/plugins/selection-style-menu` from `packages/canvas` => 4 files passed, 12 tests passed.
- 2026-04-23: Full canvas run attempted: `bunx vitest run tests` from `packages/canvas` => 37 files passed, 3 failed, 138 tests passed, 4 failed. Remaining failures are outside S47 ownership: `tests/plugins/shape1d/Shape1d.plugin.test.ts` compile error from duplicate `element` const, plus text/session editing assertions in `tests/plugins/text/Text.click-create.plugin.test.ts` and `tests/plugins/text/Text.viewport-editing.plugin.test.ts`.

---
