# S44 - canvas tests: services and transform lane
[Overview](../../BASED.md)

Repair the S42 fallout in canvas service tests and transform helper tests.

## Context
S42 refactored the canvas runtime services. Old tests under `packages/canvas/tests/services/**` and `packages/canvas/tests/plugins/transform/**` still include assumptions from removed `CanvasRegistryService` and the old editor-owned state shape.

Out of scope for this lane:
- WidgetManagerService and widget tests. The widget manager service should not be part of this test fix yet.

Current baseline from 2026-04-23:
- `bunx vitest run tests` in `packages/canvas`
- 40 files total
- 148 tests total
- 8 failing files
- 11 failing tests

Known failures in this lane:
- `tests/services/RenderOrderService.test.ts`
- `tests/plugins/transform/fx.proxy-bounds.test.ts`

## Plan
1. Update service mocks and expectations to match `ElementService`, `GroupService`, `RenderOrderService`, `SceneService`, `SessionService`, and `ToolService`.
2. Fix transform helper portal setup so tests provide current scene/session dependencies.
3. Remove stale assertions that only describe deleted service wiring.
4. Add small missing tests only when S42 introduced meaningful behavior that is otherwise untested.
5. Run the lane command and log the result.

## TODOS
- [x] Fix `tests/services/**` stale service expectations.
- [x] Leave WidgetManagerService and widget tests untouched.
- [x] Fix `tests/plugins/transform/**` stale portal and session setup.
- [x] Remove obsolete tests that target deleted runtime paths.
- [x] Run `bunx vitest run tests/services tests/plugins/transform` from `packages/canvas`.
- [x] Update this file with the focused result.

## NOTES
- Keep changes scoped to services and transform tests.
- Do not modify unrelated plugin tests in this lane.

### LOGS
- 2026-04-23: Lane created from S42 test repair split.
- 2026-04-23: Updated RenderOrderService group test to mark group nodes with `VC_NODE_KIND_ATTR` instead of relying on removed registry wiring.
- 2026-04-23: Updated transform proxy bounds tests to pass the current `scene` portal shape.
- 2026-04-23: `bunx vitest run tests/services tests/plugins/transform` passed in `packages/canvas`: 13 files, 71 tests.

---
