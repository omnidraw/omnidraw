# S61 - remove filesystem widget (comment out only)
[Overview](../BASED.md)

Disable the filesystem widget surface without deleting the implementation yet.

## Context

The filesystem widget should be removed from the active UI/runtime path for now, but the code should remain available behind comments so it can be restored or referenced during follow-up cleanup.

Relevant areas to inspect:
- widget registration/discovery
- filesystem plugin/widget wiring
- any menu or command that creates filesystem widgets

## Plan

### Disable entry points

Comment out registration, creation actions, and menu/discovery entries that expose filesystem widgets.

### Preserve implementation

Do not delete files or migrate data in this task. Leave clear comments explaining the temporary disablement.

### Validate absence

Confirm the filesystem widget is no longer offered or instantiated from normal UI paths.

## TODOS

- [ ] Find filesystem widget registration and creation entry points.
- [ ] Comment out active filesystem widget registration/exposure only.
- [ ] Add short comments at disabled entry points referencing S61.
- [ ] Verify existing filesystem widget code is not deleted.
- [ ] Smoke test that filesystem widget is no longer creatable from the UI.

## NOTES

- Scope is intentionally comment-out only, not deletion.

### LOGS

---
