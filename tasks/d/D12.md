# D12 - PRD refactor characterization guardrails (order 1/7)

[Overview](../BASED.md)

Add focused characterization tests for the accepted PRD implementation so the
following improvement refactors preserve current contracts without redesigning
behavior.

## Context

The PRD implementation and its broad acceptance matrix are complete. This task
adds only missing, high-value guardrails around contract duplication, replay and
rate-limit decisions, private RPC metadata, and Canvas resource lifetime.

Authorities:

- [`docs/PRD.md`](../../docs/PRD.md)
- [`docs/PRD_REVIEW.md`](../../docs/PRD_REVIEW.md)
- [`docs/internal/llm.app-architecture.md`](../../docs/internal/llm.app-architecture.md)
- [`docs/internal/llm.widget-system.md`](../../docs/internal/llm.widget-system.md)

## Plan

No mockup is needed because product and visual behavior are unchanged.

1. Inventory existing focused and conformance coverage before adding tests.
2. Add canonical widget-contract and private-RPC parity guardrails only where
   current evidence can drift silently.
3. Characterize exact agent history/event replay and widget-state rate-limit
   decisions.
4. Characterize missing Canvas partial-startup, replacement, interruption,
   teardown, cleanup-error, and release boundaries.
5. Run the focused suites and affected workspace typechecks.

## TODOS

- [x] Audit existing coverage and select only missing high-value cases.
- [x] Add SDK/backend portable widget-contract parity or inventory evidence.
- [x] Add exact agent history/cursor/replay and widget-state limiter guardrails.
- [x] Add private RPC inventory, codec, cursor, and idempotency guardrails.
- [x] Add missing Canvas lifecycle and resource-release guardrails.
- [x] Run focused tests and relevant typechecks.
- [x] Record protected follow-on workstreams and close the task.

## NOTES

- Production behavior is frozen for this task; prefer tests over seams.
- Public runtime source must not change, so no package version bump is expected.

### LOGS

- 2026-08-13: Created the characterization task, confirmed the worktree was
  clean, and began parallel read-only coverage audits across portable/RPC,
  agent/rate-limit, and Canvas lifecycle contracts.
- 2026-08-13: Reused the existing broad EventBus, RPC generation, Canvas
  document, and runtime suites. Added only missing exact parity and boundary
  cases: public SDK vectors against backend canonicalization; duplicated
  Capsule contract bytes; exact agent history/event order; limiter capacity,
  expiry, release, and retry timing; all six private stream cursor mappings;
  Canvas idempotency admission; operation codec attachment; partial startup
  release/reacquisition; pending-boot disposal; and ordered cleanup aggregation.
- 2026-08-13: Focused verification passed: 27 backend tests, 30 private
  transport/frontend tests, and 68 Canvas lifecycle/document tests. Backend,
  frontend, SDK, and Canvas typechecks all passed. `git diff --check` passed.
  Production behavior was unchanged; only the two pure RPC metadata helpers
  were exported as a narrow test seam.

---
