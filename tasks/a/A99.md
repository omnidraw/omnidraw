# A99 - qualify recovered canvas and updated Capsule Preview diagnostics
[Overview](../BASED.md)

Finish the qualification work left after the A94 canvas recovery and A96 live
Preview implementation. Prove the recovered canvas against the current screen
atlas, integrate Capsule's released public guest-runtime-location
observability API without crossing package boundaries, and measure Preview and
exact-promotion latency on a named reference machine.

## Context

[A94](./A94.md) and [A96](./A96.md) are implemented and merged:

- A94's canvas shell, interactions, widget placement, and AI Chat recovery
  landed through `b6ec8009`.
- [S121](../s/S121.md) restored durable clipboard, drop, and file-picker image
  insertion through Cangine's controlled image port.
- A96's durable live Preview and exact-promotion implementation landed through
  `a344095b`; [B67](../b/B67.md) later hardened its construction identity and
  added a headless authoring scenario.
- [A98](./A98.md) completed the remaining connector style controls and updated
  the selection-style atlas capture.

The remaining work was split across the two old tasks and therefore looked
like unfinished implementation:

1. A94 did not record one final live comparison of canvas atlas states `10`
   through `17` or one consolidated post-recovery gate run.
2. A96 could not report verified guest runtime source locations because the
   released Capsule packages through `0.10.0` expose no safe public runtime
   event carrying a bounded generated module/line/column.
3. A96 recorded useful single-run timings through B67 but did not run a
   reference-machine sample large enough to claim the target p95 budgets.

Capsule accepted this requirement as upstream task `A24 - observability: add
verified guest runtime locations`. Its planned public contract uses
`capsule-mount-error-v2`, exact artifact/runtime provenance, an optional
verified generated location, a mount-time startup listener, and the existing
handle listener for post-mount failures. Capsule still owns only sandbox
execution and generated-runtime observation; Vibecanvas owns compilation,
source maps, and authored-source mapping.

Do not implement the source-location lane until an immutable released package
and its final public contract are available. Capsule A24 itself does not
authorize publishing or tagging a release. Do not import Capsule source,
`dist` files, or private package paths, and do not infer a location from
message text or guest-visible stacks.

## Plan

```mermaid
flowchart LR
  Baseline["Merged A94 + A96 + S121 + A98"] --> Visual["Atlas states 10–17"]
  Baseline --> Gates["Consolidated product gates"]
  Capsule["Released Capsule mount-error-v2 API"] --> Contract["Public consumer contract"]
  Contract --> Runtime["Bounded generated runtime location"]
  Runtime --> Map["Trusted retained source-map mapping"]
  Map --> Diagnostic["Normalized widget:// diagnostic"]
  Baseline --> Warmup["Reference-machine warmup"]
  Warmup --> Samples["Preview edit + Publish samples"]
  Samples --> P95["Recorded p50/p95/max and budgets"]
  Visual --> Close["Qualification report"]
  Gates --> Close
  Diagnostic --> Close
  P95 --> Close
```

No new product mockup is useful. This task verifies the existing visual
contract in [`SCREENS.md`](../../docs/internal/screens/SCREENS.md); it should
replace an atlas image only when the live product has materially changed, not
add duplicate qualification screenshots.

### 1. Qualify the recovered canvas against the atlas

- Start from a clean, production-wired local home with clearly fake data.
- Use one recorded desktop viewport and browser/device-scale configuration for
  all full-canvas comparisons.
- Exercise atlas states `10` through `17`: populated widget, connector selection
  styles, widget actions, canvas maximize/restore, AI Chat, AI settings, live
  Draft Preview, and direct published/Draft placement.
- Include durable image insertion in the populated-canvas pass: import a local
  image through one public Cangine controller, manipulate it before upload
  settles when practical, reload, and confirm the durable media URL resolves.
- Check keyboard ownership, selection chrome, toolbar layout, widget focus,
  Preview Manage actions, last-known-good behavior, and persistence while the
  corresponding state is open.
- Compare each live state with the atlas. Update and optimize an existing WebP
  only when the live product is now authoritative and materially different.
  Record the viewport, inspected states, differences, and resulting asset
  changes in this task.

### 2. Consume Capsule's public guest-runtime-location contract

- Wait for a released Capsule version and record its version, immutable package
  identity, public root export, `capsule-mount-error-v2` schema, safety bounds,
  coordinate convention, and upstream conformance evidence.
- Upgrade all coordinated Capsule consumers and the lockfile together. Keep
  package-boundary tests fail-closed against private imports.
- Add a consumer contract test for the exact public runtime-location type,
  mount-time `onError` startup boundary, and post-mount `handle.onError`
  subscription before adapting Vibecanvas runtime code.
- Accept locations only from a verified v2 event whose `artifactHash`,
  `runtimeGeneration`, and lifecycle generation match the exact mounted Preview
  artifact/revision. Require an eligible artifact-relative generated module, a
  1-based positive safe-integer line, and a 0-based non-negative safe-integer
  UTF-16 column. Reject or omit stale, malformed, internal, cross-generation,
  cross-artifact, cross-revision, and non-Preview locations.
- Retain source maps as trusted authoring metadata outside the guest artifact.
  Map the generated location at the trusted host/service edge and emit only a
  normalized `widget://` authored file plus bounded line and column.
- Never expose host paths, environment values, dependency source, source-map
  contents, arbitrary guest text, or credentials. Preserve the existing safe
  diagnostic code/message behavior when no verified location is available.
- Prove build failures remain visible and that startup/module, synchronous guest
  callback, asynchronous guest callback, capability, and channel failures
  remain visible and queryable in the owning Preview. Only guest-runtime
  failures with verified location and provenance gain source fields.
- Update the widget architecture document and authoring prompt so they describe
  the released runtime-observability API rather than Capsule `0.9.4`'s
  limitation. Keep compilation and source-map ownership explicitly outside
  Capsule.

### 3. Measure Preview and exact-promotion latency

- Define the reference machine before sampling: hardware, OS, filesystem,
  Node/npm/Bun versions, Capsule identity, build runner, browser, viewport, and
  whether caches are cold or warm.
- Reuse the permanent B67 counter scenario and production-wired Preview path.
  Add instrumentation only at stable product boundaries and keep it out of
  product output.
- Separate cold dependency installation from the warm source-edit budget.
  Confirm an unchanged input does not rebuild and a dependency edit explicitly
  installs again.
- After warmup, collect at least 30 successful warm source-only edit-to-visible
  Preview samples and at least 30 successful current-Preview Publish samples.
  Do not discard valid slow samples. Report failures separately.
- Measure edit commit to visible `building`, edit commit to replacement
  visibility, and Publish request to durable success. Report sample count,
  p50, p95, maximum, failures, and raw bounded timing output.
- Accept warm source-only Preview at p95 under 2 seconds and exact promotion at
  p95 under 1 second. Confirm every Publish sample performs zero npm commands
  and zero guest construction runs.
- If a budget fails, retain the evidence and diagnose the dominant phase; do
  not weaken the budget or relabel a cold install as a warm edit.

### 4. Run one consolidated qualification gate

- Run focused canvas, canvas-contract, service-canvas, widget-contract,
  Capsule adapter, service-agent, database, API, ui-ai-chat, frontend, and
  widget-debug-tools tests affected by A94/A96.
- Run the relevant typechecks, functional-core lint, architecture/package
  boundary checks, production frontend build, and `git diff --check`.
- Run the Capsule browser acceptance required by the upgraded public API and
  the live browser atlas/Preview checks above.
- Record exact commands, versions, counts, skips, failures, and follow-up fixes
  in this task. Do not close from inherited green results alone.

## TODOS

- [x] Record the reference machine and deterministic qualification fixture.
- [x] Verify live atlas canvas states `10` through `17` at one viewport.
- [x] Verify durable public-controller image insertion and reload.
- [x] Record or update only materially changed screen-atlas assets.
- [x] Adopt the released Capsule public `capsule-mount-error-v2`
      guest-runtime-location API.
- [x] Add strict public-contract and package-boundary coverage.
- [x] Bind startup and post-mount events to the exact Preview artifact,
      runtime generation, lifecycle generation, and revision.
- [x] Map verified generated locations to normalized authored `widget://`
      diagnostics at the trusted edge.
- [x] Prove safe visible/queryable runtime diagnostics and rejection cases.
- [x] Collect at least 30 warm Preview and 30 exact-Publish timing samples.
- [x] Report p50, p95, maximum, failures, raw timings, and build-call counts.
- [x] Meet or explicitly diagnose the Preview and Publish latency budgets.
- [x] Run and record the consolidated qualification gate.
- [x] Update architecture, prompts, `SCREENS.md` when needed, and `FILES.md`
      after implementation files settle.

## Acceptance criteria

- Current canvas states `10` through `17` match the authoritative atlas at one
  recorded desktop viewport, or the atlas is deliberately updated with
  optimized, fake-data captures and an explanation of each material change.
- Clipboard, drop, or file-picker image insertion renders immediately, remains
  editable while pending, persists only after upload, and resolves after reload.
- Vibecanvas consumes only a released public Capsule
  `capsule-mount-error-v2` runtime-location API and keeps private-package import
  guards intact.
- A guest runtime failure can produce an exact, revision-bound authored
  `widget://` file/line/column only through matching Capsule artifact/runtime
  provenance, a verified bounded generated location, and trusted Vibecanvas
  source-map mapping.
- Initial module failures arrive through the public mount-time startup listener;
  later guest failures arrive through the public handle listener, without
  losing ordering or associating an event with a stale Preview generation.
- Missing or invalid locations degrade safely to the existing structured
  diagnostic without invented source data or sensitive host information.
- At least 30 successful warm edit samples demonstrate Preview p95 below two
  seconds, and at least 30 successful exact-promotion samples demonstrate
  Publish p95 below one second on the recorded reference machine.
- Exact promotion performs no npm command or guest construction, and cold
  installs are reported separately from the warm budget.
- The consolidated tests, typechecks, architecture checks, production build,
  browser acceptance, and live product qualification pass with all skips and
  environment limitations recorded.

## NOTES

- This is a qualification and public-API integration task, not permission to
  redesign the recovered canvas or Preview product.
- Capsule A24 owns generic sandbox runtime observation, not widget compilation
  or source-map mapping. Capsule `0.10.1` released that public API and removed
  the upstream block; Vibecanvas continues to own compilation, retained maps,
  and authored-source projection.
- Keep A94 and A96 closed. Any defect found while qualifying should receive a
  focused bug task or an explicitly logged fix here; do not reopen the broad
  recovery tasks.

### LOGS

- 2026-07-29: Created after auditing the two lingering in-progress entries.
  Confirmed their implementations are merged, reconciled A94's image work with
  S121 and its connector work with A98, and moved final visual/gate
  qualification plus A96's Capsule API and latency work into this task.
- 2026-07-29: Recorded that Capsule is actively updating its public authoring
  diagnostic API. The integration remains fail-closed until an immutable
  release exposes the required bounded location contract.
- 2026-07-30: Capsule accepted upstream A24 with a planned
  `capsule-mount-error-v2` contract: optional verified generated module,
  1-based line, 0-based UTF-16 column, exact artifact hash and runtime
  generation, mount-time startup observation, and post-mount handle
  observation. Clarified that Capsule owns only runtime observation while
  Vibecanvas owns compilation, retained source maps, and authored-source
  mapping. The integration still waits for the immutable public release.
- 2026-07-30: Unblocked on the immutable `@omnidraw/capsule@0.10.1`
  tarball. The lock records integrity
  `sha512-+dX5eKV6+igf6XmTJR4b+6+8MLScG36OnfjWzTVU9fms94CpxKLeBSGM7zTuD5cHR7vHuM+iv+Ai5VnTkCNS4g==`.
  The public root exports `CAPSULE_MOUNT_ERROR_FORMAT`,
  `CapsuleMountErrorEvent`, and `CapsuleRuntimeLocation`; the mount options and
  handle expose the startup and post-mount `onError` boundaries. The released
  v2 event carries no guest message, binds guest runtime events to an exact
  artifact hash plus positive never-reused runtime/lifecycle generations, and
  bounds a candidate location to a 1–1024-character generated module, positive
  safe-integer 1-based line, and non-negative safe-integer 0-based UTF-16
  column. Capsule omits ineligible, stale, malformed, internal, or unavailable
  locations. Root-contract tests and the release browser conformance gate pass.
- 2026-07-30: Upgraded the three coordinated direct Capsule consumers and
  lockfile to exact `0.10.1`. Public-boundary coverage continues to reject deep
  or private Capsule imports. Added hidden Vite source maps to the trusted
  distribution-build result, removed them before Capsule construction, and
  retained a digest-bound, revision/artifact-bound
  `vibecanvas.widget-source-maps.v1` envelope as Preview metadata. The envelope
  is limited to 16 MiB total, 4 MiB per map, 1,024 authored paths/maps, safe
  relative names, and exact SHA-256 provenance. Construction contract v3,
  exact-promotion identity, durable Preview storage, retention, API transport,
  and browser verification all include the optional source-map digest without
  adding the maps to the guest artifact.
- 2026-07-30: Added the trusted runtime mapping lane. Only v2 Preview VM events
  matching the mounted Capsule artifact, draft revision, runtime generation,
  and lifecycle generation can use the retained map. Eligible generated
  modules map through `@jridgewell/trace-mapping`; only one allowlisted authored
  path may leave the boundary as `widget://<path>` with positive one-based
  product line/column. Missing maps and malformed, internal, dependency,
  ambiguous, stale, cross-artifact, cross-runtime, cross-lifecycle,
  cross-revision, and published events retain the existing safe structured
  diagnostic without source fields. Tests cover both the mount-time listener
  and the handle listener, ordering, accepted mapping, and those rejection
  paths.
- 2026-07-30: Qualified one isolated production-wired fake-data home at
  1440×900 CSS pixels, device scale 1, Chromium through Playwright 1.61.1, and
  the light theme. The fixture contained the permanent B67 Counter Draft
  Preview, exact Published placement, direct Draft placement, AI Chat frame,
  two styled connectors, and a durable fake image. Live states `10`–`17`
  matched the current WebP atlas for populated widgets, selection/style
  chrome, traffic-light actions, maximize/restore, AI Chat/settings, Preview
  Manage actions, and direct Published/Draft placement. Keyboard ownership,
  sidebar visibility, last-known-good replacement during a queued build, and
  geometry/content persistence survived reload. No material visual drift was
  found, so no atlas asset or `SCREENS.md` content changed. Browser semantic
  control has no pointer-drag primitive and the desktop-control safety layer
  declined the Codex window, so the fake AI Chat frame was inserted through
  the production CanvasService command boundary instead.
- 2026-07-30: Durable image qualification exposed and fixed a recovered-canvas
  defect: `CanvasDocumentService` registered durable image descriptors but did
  not start the registration-owner preload, leaving a healthy durable URL
  visually blank after insertion/reload. The service now starts preload and
  reports failure through its existing error edge; the focused regression
  asserts that registration. The controlled image port produced an immediate,
  editable canvas node, upload settlement persisted its media identity, and
  reload rendered it. The durable URL returned HTTP 200 `image/png`,
  `private, no-store`, the expected ETag, and exactly 18,409 bytes whose
  SHA-256 matched the imported fake image.
- 2026-07-30: Reference sampler machine: MacBook Pro `MacBookPro18,3`, Apple M1
  Pro (10 cores: 8 performance, 2 efficiency), 16 GiB memory, macOS 26.2
  (25C56), Darwin 25.2.0 arm64, APFS, Bun 1.3.14, Node 23.9.0, npm 10.9.2,
  Vite 8.1.4, Capsule 0.10.1 at the integrity above, Playwright 1.61.1
  Chromium, 1440×900/device-scale-1 viewport, and repository baseline
  `651435b834cf3cd383c0d044213d3e5df260503b`. The permanent
  `apps/widget-debug-tools/scenarios/b67-counter.jsonl` fixture ran through the
  production authoring/Preview path. Its cold pass recorded create 10.749 s,
  initial validation 20.013 s, unchanged validation 28.08 ms with no rebuild,
  warm source validation 925.69 ms with no install, dependency edit/install
  808.23 ms followed by 20.255 s validation with an install, bounded error
  406.54 ms, repair reuse 25.13 ms, and final Preview 286.20 ms.
- 2026-07-30: Used a developer-only `a99-latency` sampler at stable authoring
  events, then removed it after retaining the raw evidence below. An explicit
  source-edit warmup performed one guest
  construction, one distribution build, and one dependency install. The first
  attempted run diagnosed that sampler process's cold workspace: sample 1
  performed an install and measured 12,649.99 ms to `building` and 14,398.09 ms
  to replacement. It was classified as cold rather than relabeled as a warm
  sample. A fresh independently warmed run then retained all 30 valid samples:
  zero failures; commit→visible-building p50 405.72 ms, p95 477.55 ms, max
  484.29 ms; commit→replacement-visible p50 835.04 ms, p95 997.82 ms, max
  1,074.97 ms; Publish-request→durable-success p50 39.94 ms, p95 69.91 ms,
  max 82.11 ms. The sampled Preview lane performed 30 guest constructions,
  30 distribution builds, and zero installs. All 30 exact Publish operations
  performed zero npm commands, zero distribution builds, and zero guest
  constructions. Both budgets pass.
- 2026-07-30: Raw warmed sampler output below is
  `sample revision-prefix building-ms replacement-ms publish-ms`:

  ```text
  01 d02ab3b993b4 450.34  894.13   60.80
  02 c95646e46e41 404.59  845.27   49.57
  03 7ed85f23920b 408.25  829.70   37.63
  04 37439c86484c 406.74  835.04   38.32
  05 a723f0c49fc0 401.81  822.34   48.49
  06 dd1c5272e2fa 412.32  833.15   39.75
  07 5cef0168caa1 392.14  818.82   39.47
  08 571f52f22e16 417.27  851.43   37.46
  09 c7b204dd9cc2 421.83  848.68   39.94
  10 665432905f01 403.13  826.97   39.01
  11 f6c0f8f784f4 402.78  823.30   37.60
  12 5aba7e8f96a5 405.72  817.50   37.19
  13 580cb52089f4 400.73  819.85   41.19
  14 bd103faf42e8 446.07  927.07   41.93
  15 1fe588cea8c6 406.56  910.45   39.74
  16 471d3de0653a 412.60  997.82   46.67
  17 f8c026fe8656 405.65  857.65   41.36
  18 5f794027acc1 403.20  868.98   44.18
  19 5be7741bad61 400.56  816.70   36.95
  20 d706f4fcbeec 409.57  827.52   39.56
  21 dcfaa371e3ff 403.25  830.82   36.69
  22 d4b09f71a5e8 401.14  819.74   39.42
  23 aedb8b479463 397.12  813.99   36.92
  24 b74449daa08c 400.50  831.00   41.51
  25 02d09d67472f 410.68  856.12   40.87
  26 c49c85a5c063 405.69  925.05   82.11
  27 585dede5e84e 427.30  930.29   48.20
  28 84ced7c1f8dd 477.55 1074.97   69.91
  29 a75314322826 410.97  884.73   44.52
  30 67970571bcad 484.29  928.20   42.79
  ```

- 2026-07-30: Consolidated gate completed from this branch. Tests passed:
  canvas-contract 9, canvas 107, service-canvas 12, widget-contract 47,
  Capsule adapter 36, service-agent 100, service-db 148, API 50,
  ui-ai-chat 273 across 44 files, frontend 42, and CLI 185; all recorded suites
  had zero failures or skips. The API pass corrected one stale fixture field
  and its exact route-count sentinel from 117 to the current contract/router
  count 113. The UI run emitted 12 expected jsdom
  `Window.scrollTo not implemented` warnings. Relevant typechecks passed for
  canvas-contract, canvas, service-canvas, widget-contract, Capsule adapter,
  service-agent, ui-ai-chat, widget-debug-tools, SDK, and browser acceptance.
  `bun run lint:functional-core`, `bun run test:architecture` (13 tests),
  `bun run test:external-composition` (2 tests), the production frontend build,
  and `git diff --check` passed. The Capsule browser acceptance regenerated ten
  signed fixtures and passed its one Playwright test with 28 underlying
  assertions; its expected large-bundle warning was the only build warning.
  `FILES.md` was regenerated after the implementation files settled.
- 2026-07-30: Post-review hardening replaced the inherited 0.10.0 package
  digest with the twice-reproduced 0.10.1 tarball SHA-256
  `2d0333e25c1af76df4131846f0afa10fac4023925f695c44b81fdcfd30d3534f`,
  parsed each retained source map once at the verified artifact boundary, and
  enforced the product diagnostic coordinate ceiling at both authored mapping
  and Preview normalization boundaries. The focused 25-test regression set and
  complete 274-test UI suite passed, as did UI/browser-acceptance typechecks,
  all 185 CLI tests, the one-test Capsule browser acceptance, functional-core
  lint, and `git diff --check`.
- 2026-07-30: Removed the completed task-specific latency harness from
  `widget-debug-tools`, kept its raw evidence in this task, collapsed duplicate
  Preview artifact transport schemas, and moved source-map parsing out of
  per-event host orchestration into one-time artifact verification. This
  removed 361 net lines from the implementation diff. The complete UI
  (274), Capsule adapter (36), API (50), and CLI (185) test suites passed; the
  Capsule browser acceptance passed; UI, Capsule adapter, browser acceptance,
  and widget-debug-tools typechecks passed; functional-core lint and
  `git diff --check` passed.
- 2026-08-01: Re-audited the completed qualification before checking off the
  task index. The public runtime-location and trusted source-map path remain
  intact on Capsule 0.10.2. The 36 Capsule adapter tests, 25 focused UI
  diagnostic tests, and 100 service-agent tests passed, together with the
  Capsule adapter and UI typechecks. The recorded visual, browser, build, and
  30-sample Preview/Publish latency evidence remains complete.

---
