# A64 - cli: uninstall command removes binary and data
[Overview](../../BASED.md)

Add a CLI uninstall option that removes the installed Vibecanvas binary and all local Vibecanvas data.

## Context
The curl installer places the binary under `~/.vibecanvas/bin` by default and stores local app data/config/state/cache under the configured XDG/Vibecanvas paths. Users need a supported way to fully remove Vibecanvas from a machine.

Relevant files:
- `scripts/install.sh`
- `apps/cli/src/plugins/cli/cmds/`
- `apps/cli/src/config`

## Plan
1. Decide command shape, likely `vibecanvas uninstall` with confirmation and `--yes`.
2. Remove the installed binary and install directory when owned by Vibecanvas.
3. Remove local Vibecanvas config/data/state/cache directories, including database migrations and app data.
4. Avoid deleting user project files or unrelated parent directories.
5. Print clear summary and warnings before destructive actions.
6. Add tests/docs for dry-run/confirmation behavior if supported.

## TODOS
- [x] Add CLI command/help entry for uninstall.
- [x] Implement confirmation guard and `--yes` non-interactive mode.
- [x] Remove curl-installed binary location safely.
- [x] Remove Vibecanvas-owned data/config/state/cache directories safely.
- [x] Add tests for path selection and safety guards.
- [x] Document uninstall usage.

## NOTES
- This is destructive: require explicit confirmation unless `--yes` is passed.
- Do not remove arbitrary parent XDG directories; only Vibecanvas-owned paths.

### LOGS
- Created task.
- Implemented `vibecanvas uninstall` with `--dry-run`, interactive confirmation, and `--yes`.
- Added uninstall planning/removal helpers plus focused tests for safe path selection and deletion behavior.
- Documented uninstall usage in README and installation docs.
- Verified focused CLI tests and functional-core lint. CLI package typecheck still has unrelated baseline failures.

---
