# A92 - canvas: coordinated hard cutover to CanvasService
[Overview](../BASED.md)

Cut API, CLI, browser runtime, widgets, and product tools over to CanvasService
and Cangine nodes. The cutover is a hard database boundary: no Automerge data
migration, preservation path, compatibility authority, or migration warning is
required. Remaining user-facing gaps are tracked in A94.

## Recovery references

- Pre-cutover product source: commit `396e9b45`
- Authority-cutover checkpoint: commit `3db71817`
- Visual contract: `docs/internal/screens/SCREENS.md`, especially canvas screens
  `10` through `17`

## Plan

1. Retain the completed typed snapshot/query/command/event API and CLI cutover.
2. Restore presentation and product-policy files from the pre-cutover source as
   reference, not as an Automerge rollback.
3. Port grid, camera, toolbar, drawing, transform, selection, styles, context
   menu, images, gestures, and keyboard behavior to direct Cangine/editor APIs.
4. Port widget frames, placement, portals, runtime mounting, draft Preview, and
   AI chat canvas extensions to CanvasDocumentService and WidgetStateService.
5. Track remaining visual and behavioral work in A94 without retaining the old
   authority as a compatibility path.

## Non-regression matrix

- Persistent app sidebar and canvas shell
- Grid, camera pan/zoom, drawing toolbar, shortcuts, and tool defaults
- Selection transformer, contextual styles, clone/group/delete/order
- Image placement and retained resource behavior
- Widget frame actions, maximize/restore, direct placement, and state
- AI chat, provider settings, draft Preview, and widget-create flows
- Accessibility, lifecycle teardown, reconnect, restart, and tenant switching

## TODOS

- [x] Cut snapshot/query/command/subscription API to CanvasService
- [x] Add CanvasDocumentService and local guarded undo/redo
- [x] Cut CLI list/query/mutation/dry-run commands to CanvasService
- [x] Reset the development database onto the new schema
- [x] Complete the retained-file classification and adapter map
- [x] Restore and port the retained canvas and widget runtime foundations
- [x] Remove Automerge and the compatibility persistence path
- [x] Verify focused authority, widget-host, database, build, and type gates
- [-] Preserve or migrate pre-cutover database contents
- [-] Require two-client restart, multi-browser gesture, or leak qualification

## Acceptance criteria

- Durable edits pass only through CanvasService.
- Widget instance state passes only through WidgetStateService.
- No Automerge dependency, CRDT document, v1 projection authority, or permanent
  compatibility persistence path returns.
- A fresh current database is the only supported side of the cutoff.
- Remaining product gaps do not reopen the authority cutover and are owned by
  A94.

### LOGS

- 2026-07-27: Completed the authority/API/CLI foundation and database reset.
- 2026-07-27: Reopened after the first cutover replaced the product UI with a
  thin toolbar and removed widget/AI integrations.
- 2026-07-28: Closed as a hard cutover after repository audit. Explicitly
  rejected old-database preservation and removed multi-client/browser/leak
  qualification from the cutover gate.

---
