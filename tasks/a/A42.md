# A42 - add Codex hooks for functional-core checks
[Overview](../BASED.md)

Add Codex hook integration that runs the shared functional-core rules for `fn.*.ts`, `fx.*.ts`, and `tx.*.ts` files.

## Context

Codex supports command hooks through repo-local config such as `.codex/hooks.json` or `.codex/config.toml`. The desired behavior is to reuse the normalized functional-core implementation from `.pi/extensions/functional-core` after S53, not duplicate rule logic.

Relevant files after S53:
- `.pi/extensions/functional-core/`
- `.codex/hooks.json` or `.codex/config.toml`
- `.codex/hooks/`

Codex hook events to consider:
- `SessionStart` for adding rule context to the model.
- `PostToolUse` for checking files after edits or commands.
- Optional later `PreToolUse` for preflight patch validation if reliable patch previewing is implemented.

## Plan

### Phase 1 - consume shared core

Use the normalized functional-core API from S53. Do not copy validator logic into `.codex` scripts.

### Phase 2 - add hook config

Add repo-local Codex hook config and scripts. Prefer git-root-based command paths so hooks work from subdirectories.

### Phase 3 - implement feedback

For violations, return Codex-compatible JSON feedback so Codex sees the rule failures and fixes them.

### Phase 4 - document and verify

Document trust/review steps and test hook scripts with representative stdin payloads.

## TODOS

- [x] Wait for S53 or confirm a stable reusable functional-core API exists.
- [x] Add `.codex/hooks.json` or `.codex/config.toml` for functional-core hooks.
- [x] Add Codex hook script(s) under `.codex/hooks/`.
- [x] Implement `SessionStart` rule-context output.
- [x] Implement `PostToolUse` validation using the shared functional-core linter/core.
- [x] Decide whether to validate all matching files or only touched paths when Codex payload exposes them reliably.
- [x] Add local hook script smoke tests or documented manual test commands.
- [x] Document `/hooks` trust review steps for Codex users.

## NOTES

- Project-local Codex hooks only run once the `.codex/` layer and exact hook definitions are trusted.
- First version can be post-edit validation; pre-edit blocking can be a later improvement.
- Hook commands should resolve from `$(git rev-parse --show-toplevel)`.

### LOGS

---
- 2026-06-06: Confirmed S53 reusable core exists under `.pi/extensions/functional-core/core/`.
- 2026-06-06: Added project-local `.codex/hooks.json` with `SessionStart` and `PostToolUse` command hooks using git-root-resolved paths.
- 2026-06-06: Added `.codex/hooks/functional-core-session-start.ts` to emit shared rule context from `core/checks.ts`.
- 2026-06-06: Added `.codex/hooks/functional-core-post-tool-use.ts` to parse hook stdin, validate exposed edited fn/fx/tx paths with `core/lint.ts`, and return JSON feedback.
- 2026-06-06: Chose touched-path-only validation for PostToolUse because the repo has existing unrelated functional-core violations and hook payload paths are the least noisy signal.
- 2026-06-06: Added `.codex/hooks/README.md` with `/hooks` trust review notes and smoke-test commands.
- 2026-06-06: Verification: SessionStart smoke command emitted JSON, clean path payload passed, known bad path payload returned JSON lint feedback, and `bun test ./.pi/extensions/functional-core/tests/*.test.ts` passed 19 tests.
