# A66 - toolbar: group widget tools into hover flyouts
[Overview](../BASED.md)

Render widget tools that share the same group in one toolbar slot. The grouped slot has its own icon and opens member tool icons to the right on hover.

## Context
- Toolbar implementation: [Toolbar.plugin.ts](../../packages/canvas/src/plugins/toolbar/Toolbar.plugin.ts)
- Widget config will define only the group name, not the group icon.
- Group icons must be registered/owned somewhere outside widget config.
- If a group has no registered icon, use a default group icon.

## Plan
### 1. Inspect current toolbar and widget tool metadata
- Find how toolbar tools are collected, ordered, rendered, and activated.
- Find where widget tool metadata/config is defined and consumed.
- Identify the safest owner for group icon mapping outside widget config.

### 2. Define grouping behavior
- Group widget tools by group name when present.
- Render ungrouped widget tools as normal toolbar slots.
- Render grouped widget tools as one toolbar slot per group.
- Use registered group icon when available; otherwise use default group icon.

### 3. Define hover flyout UX
- On hover/focus of a grouped slot, show member tool icons pulled out to the right.
- Keep activation behavior identical to direct toolbar tool activation.
- Ensure grouped flyout works with pointer movement and does not collapse before click.

### 4. Validate interactions
- Verify single-item groups behavior is intentional: either grouped slot or direct slot.
- Verify toolbar layout remains stable with multiple groups.
- Verify no widget config starts owning group icons.

## TODOS
- [ ] Inspect toolbar rendering and widget tool metadata paths.
- [ ] Decide and document where group icon mapping/default icon belongs.
- [ ] Implement grouped toolbar slot model.
- [ ] Implement right-side hover flyout for grouped widget tools.
- [ ] Test grouped, ungrouped, missing-icon, and multiple-group cases.

## NOTES
- Keep widget config limited to group name only.
- Prefer a small local toolbar plan before touching shared widget metadata.

### LOGS
- Created task plan from request.

---
