# A50 - ai wizard: chat-driven widget generation flow
[Overview](../../BASED.md)

Design and implement a chat-driven widget wizard that drafts an actor candidate, lets the user inspect/play/edit it, then approves and generates widget files.

## Context
- User flow must stay chat-driven: user prompt -> AI actor candidate -> user verifies in Actor tab/playground -> user reprompts or edits until satisfied -> approve -> scaffold files -> auto-prompt AI to finish implementation -> show result.
- Pre-approval must not write widget files; keep actor candidate in memory/session/draft state.
- Post-approval writes real files and emits widget update events.
- Read these files before implementation:
  - `docs/internal/llm.widget-system.md`
  - `docs/external/llm.pi.sdk.md`
  - `docs/external/llm.arrowjs.md`
  - `packages/service-agent/src/AgentService.ts`
  - `packages/api-agent/src/contract.ts`
  - `packages/canvas/src/components/AiWizzard/index.tsx`
  - `packages/canvas/src/components/AiWizzard/ChatComposer/ChatComposer.tsx`
  - `packages/canvas/src/components/AiWizzard/tabs/ActorTab.tsx`
  - `local-volume/config/widgets/sdk-test/vibecanvas.json`

## Plan

### 1. Draft model and phases
- Add wizard draft state per widget/session.
- Phase 1: actor candidate only, no filesystem write access.
- Phase 2: approved actor contract, scaffold/files can be edited.
- Store important candidate fields first: `name`, `description`, `actor`, `widget.tool.label`, `widget.tool.icon`, `widget.tool.group`, `widget.tool.priority`, `widget.tool.behavior`.

### 2. Custom tools by phase
- Define phase 1 tools:
  - `vc_set_actor_candidate`: create/replace actor candidate from chat and auto-validate before saving.
  - `vc_approve_actor_candidate`: freeze current candidate, generate `vibecanvas.json`, scaffold folders/files.
- Define phase 2 tools:
  - `vc_validate_widget_files`: validate generated draft files and actor registry against `vibecanvas.json`.
  - `vc_publish_widget`: copy draft to final widget destination and reload actor definitions.
  - Built-in `read`, `edit`, and `grep` are enough for file inspection/editing.
  - No bash by default.
- Ensure `AgentService` creates Pi sessions with only the tools needed for the current phase.

### 3. Events and frontend refresh
- Emit a draft event when actor candidate changes.
- Emit `widgetupdate` with changed file paths when `vibecanvas.json` or generated files change.
- Frontend parses `vibecanvas.json` when it changes and refreshes Actor/Preview tabs.
- Consider folder watch after approval so non-tool file edits also publish `widgetupdate`.

### 4. Actor tab playground
- Show candidate actor contract before approval.
- Let user edit important fields and state machine details.
- Validate sample input payloads against `inputMsgSchema`.
- Show selected state transition and allowed target states.
- Let user approve only when candidate validates.

### 5. Scaffold and auto implementation prompt
- On approval write deterministic scaffold:
  - `vibecanvas.json`
  - `actor/functions.ts`
  - actor function stubs
  - `actor/types.ts` if needed
  - `widget/main.ts`
  - `widget/main.css`
- Auto-prompt AI to finish implementation without changing approved candidate unless user explicitly asks for contract revision.

### 6. Tests
- Test each custom tool in isolation.
- Test phase 1 session loads candidate tools and does not load fs/bash tools.
- Test phase 2 session loads file edit/read/grep tools and does not load bash.
- Test reconnect/connect result loads latest actor candidate from Pi session custom entries.
- Test candidate validation catches bad actor state/message/schema shapes.
- Test approval writes scaffold and emits `widgetupdate` including `vibecanvas.json`.
- Test UI patch flow updates candidate revision/event shape.

## TODOS
- [x] Define draft/candidate types and phase names.
- [x] Specify custom tool contracts and validation rules.
- [x] Add standalone custom tool factories under `packages/service-agent/src/tools/tool.*.ts`.
- [x] Add phase-specific tool loading helper.
- [x] Add candidate changed and widgetupdate event shapes for tools.
- [x] Store candidate records in Pi session custom entries instead of filesystem.
- [x] Return latest `actorCandidate` from `AgentService.connectWizzard()`.
- [x] Add scaffold generation helper and approval tool.
- [x] Add publish tool that copies draft to final widget directory and reloads actor service.
- [x] Add one test file per tool file.
- [x] Wire phase-specific tools into `AgentService.connectWizzard()` session creation.
- [x] Add approval custom entry so `connectWizzard()` can choose phase 1 vs phase 2 tools from session history.
- [ ] Add latest actor candidate as prompt/session context before each prompt.
- [ ] Add API/frontend approval flow around `vc_approve_actor_candidate` or equivalent service method.
- [ ] Add Actor tab playground/edit/approve UI.
- [ ] Add frontend handling for candidate and widgetupdate events.
- [ ] Add post-approval auto-prompt to finish implementation.

## NOTES
- Keep chat as the primary interaction; UI edits should update the same candidate state so future chat prompts include latest context.
- Phase 1 must be safe: no file writes, no bash.
- Phase 2 should edit generated widget files with built-in read/edit/grep only; avoid bash unless a later task explicitly allows it.
- No custom list/read/write/grep widget-file tools for now; use Pi built-ins.
- No custom patch candidate tool for now; call `vc_set_actor_candidate` again with the revised full candidate.
- Actor candidate validation is part of `vc_set_actor_candidate`.
- Candidate records are persisted via Pi `sessionManager.appendCustomEntry`, not files under the draft cwd.
- Successful approval appends a `vibecanvas.actorCandidateApproved` custom entry; phase selection uses latest approval entry.
- `connectWizzard()` includes `actorCandidate`, which is `null` on first connect and latest candidate record after generation.
- `connectWizzard()` now loads tools from `fnCreateWidgetWizardPhaseTools`; phase 1 has only candidate/approve tools, phase 2 has read/edit/grep plus validate/publish.
- Candidate schema is converted to JSON Schema from `Z_ACTOR_CANDIDATE` for the tool parameter; candidate does not include version.
- Manifest version is set programmatically to `1` for this flow.
- Approved actor contract is stable by default; changing it should be an explicit contract revision loop.
- Implemented tool files:
  - `packages/service-agent/src/tools/tool.set-actor-candidate.ts`
  - `packages/service-agent/src/tools/tool.approve-actor-candidate.ts`
  - `packages/service-agent/src/tools/tool.validate-widget-files.ts`
  - `packages/service-agent/src/tools/tool.publish-widget.ts`
  - `packages/service-agent/src/tools/fn.phase-tools.ts`
  - `packages/service-agent/src/tools/fn.candidate.ts`
  - `packages/service-agent/src/tools/fn.result.ts`
  - `packages/service-agent/src/core/fx.session-candidate.ts`
  - `packages/service-agent/src/core/tx.session-candidate.ts`
  - `packages/service-agent/src/tools/tx.scaffold.ts`
  - `packages/service-agent/src/tools/tx.widget-files.ts`
  - `packages/service-agent/src/tools/CONSTANTS.ts`
  - `packages/service-agent/src/tools/types.ts`
- Implemented tests:
  - `packages/service-agent/tests/tool.set-actor-candidate.test.ts`
  - `packages/service-agent/tests/tool.approve-actor-candidate.test.ts`
  - `packages/service-agent/tests/tool.validate-widget-files.test.ts`
  - `packages/service-agent/tests/tool.publish-widget.test.ts`
  - `packages/service-agent/tests/fn.phase-tools.test.ts`

### LOGS
- Created task from design discussion about chat-driven widget generation flow and phase-specific Pi tools.
- Added standalone tool factories and split shared draft/scaffold/publish helpers into `fn.*`, `tx.*`, `CONSTANTS.ts`, and `types.ts`.
- Added `ActorService.reload()` for publish-time definition reload.
- Added one test file per tool file; verified with `bun test packages/service-agent/tests --timeout=20000` -> 9 pass.
- Removed `vc_get_actor_candidate`; latest candidate should be injected by `AgentService` prompt/session context instead of fetched by a tool.
- Removed filesystem candidate persistence; `vc_set_actor_candidate` now appends latest candidate records through `sessionManager.appendCustomEntry`, and `connectWizzard()` returns `actorCandidate`.
- Wired `connectWizzard()` to `fnCreateWidgetWizardPhaseTools` and added approval-entry based phase selection.
- Ran `bun --filter @vibecanvas/service-agent typecheck`; new tool type errors were fixed, but command still fails on unrelated existing `service-db` SQL module/global typing issues.

---
