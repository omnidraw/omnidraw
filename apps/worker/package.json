{
  "name": "@vibecanvas/worker",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/main.ts" },
  "scripts": {
    "start": "bun run src/main.ts",
    "build": "bun build src/main.ts --target=bun --outdir=dist --entry-naming=worker.mjs",
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test tests"
  },
  "dependencies": {
    "@vibecanvas/service-db": "workspace:*",
    "@vibecanvas/service-workflow": "workspace:*",
    "@vibecanvas/shared-functions": "workspace:*",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": { "typescript": "catalog:" }
}
