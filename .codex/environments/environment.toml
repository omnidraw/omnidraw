# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "vibecanvas"

[setup]
script = '''
# - CODEX_SOURE_TREE_PATH
# - CODEX_WORKTREE_PATH

mkdir -p "$CODEX_WORKTREE_PATH/local-volume"
rsync -a \
  --exclude 'data/machine.json' \
  "$CODEX_SOURCE_TREE_PATH/local-volume/" \
  "$CODEX_WORKTREE_PATH/local-volume/"

cd "$CODEX_WORKTREE_PATH"
bun install

cd "$CODEX_WORKTREE_PATH/packages/sdk"
bun run build
'''
