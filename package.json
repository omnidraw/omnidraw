{
  "name": "vibecanvas",
  "private": true,
  "description": "Run your agents in an infinite canvas",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "catalog": {
    "@automerge/automerge": "3.2.6",
    "@automerge/automerge-repo": "2.5.6",
    "@automerge/automerge-repo-network-broadcastchannel": "2.5.6",
    "@automerge/automerge-repo-network-websocket": "2.5.6",
    "@automerge/automerge-repo-storage-indexeddb": "2.5.6",
    "@orpc/client": "1.14.7",
    "@orpc/contract": "1.14.7",
    "@orpc/server": "1.14.7",
    "@earendil-works/pi-coding-agent": "^0.80.6",
    "typescript": "7.0.2",
    "zod": "4.4.3",
    "@kobalte/core": "^0.13.12",
    "lucide-solid": "^1.24.0",
    "lucide-static": "^1.24.0",
    "solid-js": "^1.9.14",
    "@tursodatabase/database": "0.6.1"
  },
  "scripts": {
    "postinstall": "node scripts/patch-automerge-repo-throttle.mjs",
    "pretest": "bun run --filter '@vibecanvas/sdk' build",
    "test": "bun run --parallel --filter '@vibecanvas/cli' --filter '@vibecanvas/web' --filter '@vibecanvas/frontend' --filter '@vibecanvas/ai-chat' --filter '@vibecanvas/actor-ui' --filter '@vibecanvas/api-actors' --filter '@vibecanvas/api-canvas' --filter '@vibecanvas/api-db' --filter '@vibecanvas/api-file' --filter '@vibecanvas/api-filesystem' --filter '@vibecanvas/api-notification' --filter '@vibecanvas/api-pty' --filter '@vibecanvas/api-tool' --filter '@vibecanvas/runtime' --filter '@vibecanvas/service-actor' --filter '@vibecanvas/service-agent' --filter '@vibecanvas/service-automerge' --filter '@vibecanvas/service-db' --filter '@vibecanvas/service-event-publisher' --filter '@vibecanvas/service-filesystem' --filter '@vibecanvas/service-pty' --filter '@vibecanvas/shared-functions' --filter '@vibecanvas/tapable' test",
    "test:ci:docker": "docker run --rm -it --platform linux/amd64 -v \"$PWD\":/repo -w /repo ubuntu:24.04 bash -lc 'export DEBIAN_FRONTEND=noninteractive && apt-get update && apt-get install -y curl unzip git ca-certificates && curl -fsSL https://bun.sh/install | bash && export BUN_INSTALL=/root/.bun && export PATH=$BUN_INSTALL/bin:$PATH && bun --version && bun install --frozen-lockfile && bun run test'",
    "test:binary": "bun run scripts/test-binary.ts",
    "test:upgrade:local": "bun run scripts/test-local-upgrade.ts",
    "build": "bun run scripts/build.ts",
    "build:single": "bun run scripts/build.ts --single",
    "verify:turso-native-encryption": "bun run scripts/build.ts --verify-native-encryption-only",
    "verify:turso-native-encryption:host": "bun run scripts/build.ts --verify-native-encryption-host-only",
    "web:dev": "bun --filter @vibecanvas/web dev",
    "web:build": "bun --filter @vibecanvas/web build",
    "publish:npm": "bun run scripts/publish-npm.ts",
    "release:github": "bun run scripts/release.ts",
    "lint:functional-core": "eslint \"**/src/**/{fn,fx,tx}.*.ts\"",
    "lint:functional-core:agent": "bun run .codex/hooks/functional-core-eslint.ts",
    "generate:files": "bun run scripts/generate-files-md.ts",
    "dev": "bun run scripts/dev.ts",
    "client:dev": "cd apps/frontend && bun run dev",
    "server:dev": "cd apps/cli && bun run --watch src/main.ts serve",
    "server:prod": "cd apps/cli && NODE_ENV=production bun run src/main.ts serve --port 7496"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/parser": "^8.63.0",
    "bun-types": "^1.3.14",
    "eslint": "^10.7.0"
  },
  "overrides": {
    "@codemirror/language": "6.12.4",
    "@codemirror/state": "6.7.1",
    "@codemirror/view": "6.43.6",
    "prosemirror-model": "1.25.11",
    "prosemirror-view": "1.42.1",
    "vite": "8.1.4"
  },
  "dependencies": {
    "@lastolivegames/becsy": "^0.15.5",
    "handlebars": "^4.7.9",
    "pixi.js": "^8.19.0"
  }
}
