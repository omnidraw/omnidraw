{
  "name": "@omnidraw/resource-runtime",
  "version": "0.5.0",
  "description": "Portable resource capabilities, providers, and gateway contracts for Omnidraw.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vibecanvas/vibecanvas.git",
    "directory": "packages/resource-runtime"
  },
  "publishConfig": {
    "@omnidraw:registry": "https://registry.npmjs.org/",
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "type": "module",
  "files": [
    "src"
  ],
  "exports": {
    ".": "./src/index.ts",
    "./local": "./src/local/index.ts"
  },
  "scripts": {
    "build": "bun run ../../scripts/build-typescript-package.ts",
    "prepublishOnly": "bun run typecheck && bun run test && bun run build",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@omnidraw/tenant-core": "workspace:*"
  },
  "peerDependencies": {
    "@tursodatabase/database": "0.6.1"
  },
  "devDependencies": {
    "@tursodatabase/database": "0.6.1"
  }
}
