export const PREVIEW_ACTIONS_HEADER_ID = 'preview-actions';

export const PREVIEW_ACTION_IDS = Object.freeze({
  reload: 'reload',
  rebuild: 'rebuild',
  publish: 'publish',
  remove: 'remove',
} as const);
